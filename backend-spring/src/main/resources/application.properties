# Server Configuration
server.port=8080

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/tirthseva_springboot_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=cdac
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Settings (same as .NET backend)
jwt.secret=TirthSeva_Secret_Key_2024_Very_Long_And_Secure_Key_For_JWT_Token_Generation
jwt.issuer=TirthSevaAPI
jwt.audience=TirthSevaClient
jwt.expiry-minutes=30

# Email Settings (same as .NET backend)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=puranikisha@gmail.com
spring.mail.password=ffmliridlhwwyrej
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
email.sender-name=TirthSeva

# App Settings
app.frontend-url=http://localhost:5173
app.enable-email-verification=true

# File Upload Configuration
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=./uploads

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.tirthseva=DEBUG
